<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../../partials/head'); %>
  </head>
  <body class="container">
    <header><%- include('../../partials/header'); %></header>

    <main>
      <h3 class="text-center my-2 text-danger">CONFIRM DELETE VIDEO</h3>
      <form
        action="/post/<%= post._id%>/video/delete"
        method="post"
        class="w-50 mx-auto"
      >
        <video class="mx-auto my-3" width="520" height="400" controls>
          <source src="/uploads/<%= post.video%>" type="video/mp4" />
          <source src="movie.ogg" type="video/ogg" />
          Your browser does not support the video tag.
        </video>
        <p class="text-danger my-3">
          Are you sure you want to delete the above video ?
        </p>
        <button type="submit" class="btn btn-danger">Delete VIDEO</button>
      </form>
    </main>

    <footer><%- include('../../partials/footer'); %></footer>
  </body>
</html>
